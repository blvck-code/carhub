"use client";
import React from "react";

const SearchBar = () => {
  const handleSearch = () => {};
  return <form action="" className="searchBar" onSubmit={handleSearch}></form>;
};

export default SearchBar;
